# 📦 Проект: Интернет-магазин (React + Vite)

Данный проект использует модульную структуру, вдохновленную принципами **Feature-Sliced Design**, что обеспечивает легкую масштабируемость и порядок в коде.

---

## 📂 Структура проекта

### `src/` — Исходный код

* **`assets/`** — Статические ресурсы.
    * `/icons` — SVG и шрифтовые иконки.
    * `/images` — Баннеры, заглушки и элементы декора.
* **`components/`** — Общие компоненты (Shared UI).
    * `/ui` — Базовые элементы: кнопки, инпуты, модальные окна.
    * `/layout` — Каркас приложения: Header, Footer, Sidebar.
    * `/common` — Спиннеры, хлебные крошки, сообщения об ошибках.
* **`features/`** — Основная бизнес-логика по категориям.
    * `cart/` — Управление корзиной (логика + компоненты).
    * `catalog/` — Списки товаров, фильтрация и поиск.
    * `checkout/` — Процесс оформления заказа.
    * `product-details/` — Детальная информация о товаре.
* **`hooks/`** — Переиспользуемые кастомные хуки (`useLocalStorage`, `useCart`).
* **`pages/`** — Компоненты страниц (сборка из фич и UI-компонентов).
* **`services/`** — Конфигурация API и сетевые запросы (Axios/Fetch).
* **`store/`** — Управление глобальным состоянием (Redux/Zand/Context).
* **`utils/`** — Утилитарные функции: форматирование цен, дат, валидация данных.
* **`App.jsx`** — Главный файл: роутинг и провайдеры контекста.

---

## 🛠 Технологический стек

* **Frontend:** React (Vite)
* **State Management:** [Укажите ваш, например: Redux Toolkit]
* **Styling:** [Укажите ваш, например: Tailwind CSS / SCSS]
* **Routing:** React Router DOM

---

## 🚀 Как запустить

1. Установите зависимости:
   ```bash
   npm install